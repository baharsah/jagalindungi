import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'co.baharsah.jagalindungi',
  appName: 'jagalindungi',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
